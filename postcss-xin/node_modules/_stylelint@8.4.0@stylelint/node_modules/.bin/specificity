#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_specificity@0.3.2@specificity/bin/specificity" "$@"
  ret=$?
else 
  node  "$basedir/../../../_specificity@0.3.2@specificity/bin/specificity" "$@"
  ret=$?
fi
exit $ret
