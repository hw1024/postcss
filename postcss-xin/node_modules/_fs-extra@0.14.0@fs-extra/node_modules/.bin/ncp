#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_ncp@1.0.1@ncp/bin/ncp" "$@"
  ret=$?
else 
  node  "$basedir/../../../_ncp@1.0.1@ncp/bin/ncp" "$@"
  ret=$?
fi
exit $ret
